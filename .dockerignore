# Version control
.git
.gitignore
.gitattributes

# Gradle / build output
.gradle
build
out
bin

# Frontend (Vite) — rebuilt inside the image
ui/node_modules
ui/dist
# NOTE: ui/.env IS copied into the build — Vite needs VITE_* vars at build time.
# Only public (browser-safe) keys belong here, e.g. VITE_GOOGLE_MAPS_KEY.
ui/.env.local

# IDE / editor
.idea
*.iml
*.ipr
*.iws
.vscode
.settings
.classpath
.project

# Local docs
HELP.md
README.md

# OS noise
.DS_Store
Thumbs.db
